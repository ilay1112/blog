<%- include('partials/header'); %> 
<h1>Home</h1>
<p> <%= startingContent %>  </p>

<% posts.forEach(function(post) { %>
    <h2> <%= post.title %> </h2>
<% }) %>

<% posts.forEach(function(post) { %>
    <p> <%= post.content %> </p>
<% }) %>

<%- include('partials/footer'); %> 
